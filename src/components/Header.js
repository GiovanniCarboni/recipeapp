import React from "react";

export default function Header() {
  return (
    <header className="bar">
      <input type="text" placeholder="ex. Risotto alla milanese" />
      <button>Search</button>
    </header>
  );
}
